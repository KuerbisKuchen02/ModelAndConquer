package models._static;

public class DestroyableObject extends Entity implements INonPlayerEntity{
    public DestroyableObject(String name, String description) {
        super(name, description);
    }

}